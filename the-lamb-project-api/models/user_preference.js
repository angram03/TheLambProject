// const xlsx = require("xlsx");
// const db = require("../db");

// class User_Preference {
//   static async insertData(data) {
//     var workbook = xlsx.readFile(data),
//         worksheet = work.Sheet[workbook.SheetNames[0]];
//         range = xlsx.utils.decode_range(worksheet["!ref"]);
//   }
// }
